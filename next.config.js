/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.shopify.com', 'www.shreeguruji.com', 'images.unsplash.com', 'via.placeholder.com'],
  },
}

module.exports = nextConfig
